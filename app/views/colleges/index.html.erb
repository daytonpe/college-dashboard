<h1>My Favorite Colleges</h1>

<% @colleges.each do |college| %>
	<%= link_to(college.name, details_path(college.name)) %> | 
        <%= link_to 'Delete', college,
                method: :delete, data: {confirm: 'Are you sure?'} %>
	<hr>
<% end %>
<%= link_to 'Back', :back %>